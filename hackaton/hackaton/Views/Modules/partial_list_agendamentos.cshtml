@model List<hackaton.Models.Schedule>
@using VM = hackaton.Models.ViewModels

@{
    if (Model != null)
    {
        @foreach (var agendamento in Model)
        {
            <partial name="~/Views/Modules/partial_card_agendamento.cshtml" model="agendamento" />
        }
    }
}
